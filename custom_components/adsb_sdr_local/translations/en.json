{
  "config": {
    "step": {
      "user": {
        "title": "Set up ADS-B SDR Local",
        "description": "Connect to a local readsb/dump1090 JSON endpoint (aircraft.json).",
        "data": {
          "host": "Receiver IP/hostname",
          "port": "Port",
          "path": "Path to aircraft.json"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the receiver"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Filtering + map options.",
        "data": {
          "lat": "Home latitude (optional)",
          "lon": "Home longitude (optional)",
          "max_range_km": "Max range (km)",
          "min_alt_ft": "Minimum altitude (ft)",
          "military_only": "Military only (heuristic)",
          "callsign_prefixes": "Military callsign prefixes (CSV)",
          "callsign_regex": "Callsign regex (optional)",
          "hex_prefixes": "ICAO hex prefixes (CSV, optional)",
          "stale_seconds": "Remove aircraft from map after (seconds)",
          "scan_interval": "Update interval (seconds)"
        }
      }
    }
  }
}
